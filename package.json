{
  "name": "kcc-bridge",
  "version": "1.1.0",
  "description": "MCP-over-HTTP server (Cloudflare Worker) exposing the AI Native Solutions estate registry and unsigned bundle-template generators as JSON-RPC 2.0 tools.",
  "type": "module",
  "main": "worker.js",
  "author": "Simon Gant",
  "license": "MIT",
  "scripts": {
    "test": "node test.mjs"
  }
}
